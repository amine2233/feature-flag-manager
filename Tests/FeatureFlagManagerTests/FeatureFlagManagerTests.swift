import XCTest
@testable import FeatureFlagManager

final class feature_flag_managerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        // XCTAssertEqual(feature_flag_manager().text, "Hello, World!")
    }
}
